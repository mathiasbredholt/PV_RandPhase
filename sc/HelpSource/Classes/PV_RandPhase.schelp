TITLE:: PV_RandPhase
summary:: Randomizes phase
categories:: UGens>FFT

DESCRIPTION::
Randomizes phase

CLASSMETHODS::
private:: categories

EXAMPLES::
code::
(
x = {
  var sig, chain;
  sig = SinOsc.ar(200 * Duty.kr(0.25, 0, Diwhite(1, 8)));
  chain = FFT(LocalBuf(2048), sig);
  chain = PV_MagSmooth(chain, 0.99);
  chain = PV_RandPhase(chain);
  IFFT(chain).dup * 0.1
}.play;
)
::
